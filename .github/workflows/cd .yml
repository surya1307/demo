name : Docker pipeline
on: 
   push:
      branches:
          - main
jobs:
   docker-build :
     runs-on : ubuntu-Latest
steps:
    - name: checkout code
      uses : actions/checkout@v2      
    - name: Docker image build
    run : docker build -t main:v1 .

